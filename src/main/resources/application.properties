spring.application.name=contracts-api
jwt.secret=my-very-secure-secret-key-32-bytes-long

# Pour tout Spring Security
logging.level.org.springframework.security=DEBUG

# Pour le filtre JWT (si tu veux plus de détails sur les requêtes HTTP entrantes)
logging.level.ch.scorndes.contractsapi.security=DEBUG
logging.level.org.springframework.web.filter=DEBUG

-Amapstruct.defaultComponentModel=spring
-Amapstruct.suppressGeneratorTimestamp=true
-Amapstruct.verbose=true

# Connexion PostgreSQL
spring.datasource.url=jdbc:postgresql://localhost:5432/contracts_db
spring.datasource.username=scorne
spring.datasource.password=db_pg_202505
spring.datasource.driver-class-name=org.postgresql.Driver

# Hibernate
spring.jpa.hibernate.ddl-auto=validate
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true

spring.sql.init.mode=always